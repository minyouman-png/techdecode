---
import GamePage from '../../components/GamePage.astro';
import { games } from '../../i18n/games';

export function getStaticPaths() {
  return games.map((g) => ({ params: { slug: g.slug } }));
}
const { slug } = Astro.params;
---
<GamePage lang="en" slug={slug} />
